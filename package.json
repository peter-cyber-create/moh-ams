{
  "name": "moh-ams",
  "private": true,
  "version": "2.0.0",
  "description": "Ministry of Health — Activities Management System",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "preview": "npm run preview --prefix frontend"
  }
}
